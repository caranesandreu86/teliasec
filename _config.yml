# Welcome to Jekyll!
#
# This config file is meant for settings that affect your whole blog, values
# which you are expected to set up once and rarely edit after that. If you find
# yourself editing this file very often, consider using Jekyll's data files
# feature for the data you need to update frequently.
#
# For technical reasons, this file is *NOT* reloaded automatically when you use
# 'bundle exec jekyll serve'. If you change this file, please restart the server process.
#
# If you need help with YAML syntax, here are some quick references for you:
# https://learn-the-web.algonquindesign.ca/topics/markdown-yaml-cheat-sheet/#yaml
# https://learnxinyminutes.com/docs/yaml/
#
# Site settings
# These are used to personalize your new site. If you look in the HTML files,
# you will see them accessed via {{ site.title }}, {{ site.email }}, and so on.
# You can create any custom variable you would like, and they will be accessible
# in the templates via {{ site.myvariable }}.



title: "Teliasec"
description: "Soluciones tecnológicas para tu empresa"
lang: "es"
url: "https://teliasec.com"
baseurl: ""   # Mantén vacío si tu web está en la raíz del dominio.

# Tema y apariencia
remote_theme: "mmistakes/minimal-mistakes"
minimal_mistakes_skin: "dark"  # Otros: default, air, aqua, contrast, mint, neon...

# Plugins
plugins:
  - jekyll-include-cache
  # Si NO usas GitHub Pages para compilar y despliegas tú (Netlify, VPS, servidor propio), descomenta:
  # - jekyll-remote-theme
  # Recomendados para SEO y sitemap (opcionales):
  - jekyll-seo-tag
  - jekyll-sitemap

# Valores por defecto para páginas
defaults:
  - scope:
      path: ""
      type: "pages"
    values:
      layout: "single"
      author_profile: false

# Autor
author:
  name: "Teliasec"
  email: "info@teliasec.com"

# Excluir archivos que no deben compilarse
exclude:
  - README.md
  - node_modules
  - vendor
  - Gemfile
  - Gemfile.lock

